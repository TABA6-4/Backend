spring:
  application:
    name: focus
  datasource:
    url: jdbc:mysql://focus.c94y6cs2sggm.ap-northeast-2.rds.amazonaws.com/Focus
    username: admin
    password: eksrnreogkrry
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
#  security:
#    user:
#      name: user
#      password: 1234


jwt:  #터미널에 openssl rand -hex 32 입력. HS256 알고리즘 사용
  secret: 93f4e4a4f4a16bada44520c35a55b3b57a03e322dc5aa1b540b4f0c5f1ed5d89

springdoc:
  swagger-ui:
    path: /api-test  # swagger-ui 접근 경로에 대한 별칭, 해당 주소로 접속해도 http://localhost:8080/swagger-ui/index.html로 리다이렉션 됨.

    groups-order: DESC # path, query, body, response 순으로 출력

    tags-sorter: alpha # 태그를 알파벳 순으로 정렬

    operations-sorter: method  # delete - get - patch - post - put 순으로 정렬, alpha를 사용하면 알파벳 순으로 정렬 가능

  paths-to-match:
    - /api/** # swagger-ui에 표시할 api의 엔드포인트 패턴